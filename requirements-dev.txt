# ── rec-inference: development / CI dependencies ─────────────────
# These are not installed in the production container image.
# Install with: pip install -r requirements-dev.txt

# Linting & formatting
ruff==0.4.4
mypy==1.10.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0          # async test client for FastAPI

# Type stubs
types-requests==2.32.0
pyyaml>=6.0
